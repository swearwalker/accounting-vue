import { createStore } from 'vuex'

import categories from '@/store/categories'

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    categories,
  },
})
